<div class="row">
    <slot />
</div>

<style>
    .row {
        margin-block: 12px;

        &:has(+ :is(.row, .grid)) {
            margin-bottom: 0;
        }

        &:last-child {
            margin-bottom: 0;
        }
    }

    .row > :global(p) {
        margin: 0;
        display: flex;
        flex-flow: row wrap;
        gap: 12px;

        & > :global(img) {
            min-width: 0;
            height: 100%;
        }
    }

    @media screen and (min-width: 640px) {
        .row > :global(p) {
            flex-flow: row nowrap;
        }
    }
</style>
